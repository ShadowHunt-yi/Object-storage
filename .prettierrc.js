module.exports = {
    // 使用单引号而不是双引号
    singleQuote: true,
    
    // 不使用分号
    semi: false,
    
    // 每行最大字符数
    printWidth: 100,
    
    // 使用 2 个空格缩进
    tabWidth: 2,
    
    // 使用空格而不是 tab 缩进
    useTabs: false,
    
    // 对象中的括号之间使用空格
    bracketSpacing: true,
    
    // 箭头函数只有一个参数时也使用括号
    arrowParens: 'always',
    
    // HTML 元素的'>'放在最后一行的末尾，而不是单独放在下一行
    bracketSameLine: false,
    
    // 处理行尾符号，保持在不同操作系统下一致
    endOfLine: 'auto',
    
    // 不使用尾随逗号
    trailingComma: 'none',
    
    // Vue 文件中使用一致的空格和缩进
    vueIndentScriptAndStyle: false,
    
    // 配置 HTML 文件中空白的敏感性
    htmlWhitespaceSensitivity: 'css',
    
    // 在对象和数组的最后一个元素后面不添加逗号
    trailingComma: 'none',
    
    // 保持对 Markdown 文本内容的格式化
    proseWrap: 'preserve',
    
    // 是否格式化 `<script>` 和 `<style>` 内容
    embeddedLanguageFormatting: 'auto'
  }